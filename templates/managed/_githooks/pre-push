#!/bin/sh
# Managed by platform-pkg-dev - do not edit. Regenerated by `dev sync`.
#
# Deliberately thin: the logic lives in platform-pkg-dev so it can be updated centrally,
# the same way tsconfig.json extends platform-pkg-dev/tsconfig.base.json. Put this
# package's own checks in .githooks/pre-push.extend.sh instead.
#
# Paths are derived from this script's own location, not the working directory:
# git runs hooks from the repository root, which in a monorepo is above the
# package, but running this by hand from the package root must work too.
DEV_PACKAGE_DIR=$(cd "$(dirname "$0")/.." && pwd)
export DEV_PACKAGE_DIR
exec "$DEV_PACKAGE_DIR/node_modules/platform-pkg-dev/hooks/dev-hook" pre-push "$@"
